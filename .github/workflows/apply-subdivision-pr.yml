name: Apply subdivision count changes

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/show-subdivision-counts'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/show-subdivision-counts
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Fix generated template escaping
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('scripts/apply_subdivision_counts.mjs')
          text = path.read_text()
          text = text.replace(
              "return subdivisionIndex === 0 ? token : `${beatIndex + 1} ${token}`;",
              "return subdivisionIndex === 0 ? token : `\\${beatIndex + 1} \\${token}`;",
          )
          text = text.replace(
              "key={\\`${countBeatIndex}-\\${countSubdivisionIndex}\\`}",
              "key={\\`\\${countBeatIndex}-\\${countSubdivisionIndex}\\`}",
          )
          path.write_text(text)
          PY
      - name: Apply source changes
        id: apply-source
        continue-on-error: true
        run: node scripts/apply_subdivision_counts.mjs > apply-subdivision.log 2>&1
      - name: Upload apply diagnostics
        if: steps.apply-source.outcome != 'success'
        uses: actions/upload-artifact@v4
        with:
          name: subdivision-apply-diagnostics
          path: apply-subdivision.log
      - name: Fail when apply failed
        if: steps.apply-source.outcome != 'success'
        run: exit 1
      - run: rm -f .github/workflows/apply-subdivision-pr.yml
      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Show subdivision count positions"
          git push origin HEAD:agent/show-subdivision-counts
